import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    color: "black",
    fontWeight: "bold",
    marginBottom: 10,
  },
});

export default styles;
